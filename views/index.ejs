<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=SUSE:wght@100..800&display=swap" rel="stylesheet">
    <title>Bitcoin Price</title>
</head>
<body class="suse">
    <h1>Crypto Live Prices</h1>
    <div class="box">
        <div class="image">
            <img src="/bitcoin-btc-logo.png"><h2>Bitcoin Price</h2>
        </div>
        <% if (locals.bitcoin) { %>
            <p><strong>Name:</strong> <%= bitcoin.code %></p>
            <p><strong>Price:</strong> $<%= Math.round((bitcoin.rate + Number.EPSILON) * 100) / 100 %></p>
            <p><strong>Market Cap:</strong> $<%= bitcoin.cap %></p>
            <p><strong>Volume:</strong> <%= bitcoin.volume %></p>
        <% } else { %>
            <p>Bitcoin data not found.</p>
        <% } %>
    </div>

    <div class="box">
        <div class="image">
            <img src="/ethereum-eth-logo.png"><h2>Ethereum Price</h2>
        </div>
        <% if (locals.Etherium) { %>
            <p><strong>Name:</strong> <%= Etherium.code %></p>
            
            <p><strong>Price:</strong> $<%= Math.round((Etherium.rate + Number.EPSILON) * 100) / 100 %></p>
            <p><strong>Market Cap:</strong> $<%= Etherium.cap %></p>
            <p><strong>Volume:</strong> <%= Etherium.volume %></p>
        <% } else { %>
            <p>Ethereum data not found.</p>
        <% } %>
    </div>

    <div class="box">
        <div class="image">
            <img src="/tether-usdt-logo.png"><h2>USTD Price</h2>
        </div>
        <% if (locals.USDT) { %>
            <p><strong>Name:</strong> <%= USDT.code %></p>
            <p><strong>Price:</strong> $<%= Math.round((USDT.rate + Number.EPSILON) * 100) / 100 %></p>
            <p><strong>Market Cap:</strong> $<%= USDT.cap %></p>
            <p><strong>Volume:</strong> <%= USDT.volume %></p>
        <% } else { %>
            <p>USDT data not found.</p>
        <% } %>
    </div>

    <div class="box">
        <div class="image">
            <img src="/bnb-bnb-logo.png"><h2>BNB Price</h2>
        </div>
        <% if (locals.BNB) { %>
            <p><strong>Name:</strong> <%= BNB.code %></p>
            <p><strong>Price:</strong> $<%= Math.round((BNB.rate + Number.EPSILON) * 100) / 100 %></p>
            <p><strong>Market Cap:</strong> $<%= BNB.cap %></p>
            <p><strong>Volume:</strong> <%= BNB.volume %></p>
        <% } else { %>
            <p>BNB data not found.</p>
        <% } %>
    </div>
</body>
</html>
